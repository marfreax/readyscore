# V8.4 → V8.5 Handoff

## Accepted

- EQ V2 uses 24 scenario single-choice items.
- Four EQ dimensions × six items.
- Each item has four plausible options.
- Each item has an explicit hidden ordinal scoring key with values 1–4.
- Option position is not a global score mapping.
- EQ scoring is distinct from Cognitive correct/incorrect scoring.
- EQ V2 configuration/scoring/interpretation/selection/taxonomy/result boundaries are versioned.
- Historical EQ V1 content remains immutable.
- Customer runtime does not expose scoring keys.
- No clinical EQ claim is introduced.

## Next

V8.5 DISC Instrument must independently implement the locked scenario forced-choice model. It must not reuse EQ's ordinal scoring as if DISC options were universally ordered.
