# F.10-C.1 Correction

The first F.10-C.1 ZIP contained two integration mistakes:

1. `AssessmentRunner.tsx` contained literal `\n` characters instead of newlines.
2. Runtime-service imports used paths one or two levels too deep.

Both are corrected in this ZIP against the actual project tree.

No assessment contract, scorer, question-bank lifecycle, or database mutation was changed.
